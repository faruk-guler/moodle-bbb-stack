(self-signed) bir sertifika oluşturmak için:

openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
   -keyout bbb.guler.com.key \
   -out bbb.guler.com.crt \
   -config csr.conf

-----------
bbb.guler.com.key   → private key
bbb.guler.com.crt   → SSL certificate
-----------

2: Nginx için Diffie-Hellman (DH) Parametresini Unutmayın:
mkdir -p /etc/nginx/ssl/
openssl dhparam -out /etc/nginx/ssl/dhp-4096.pem 4096

3: Sertifikayı Cliente ve Servere Tanıtın:

Server:
cp /etc/nginx/ssl/bbb.guler.com.crt /usr/local/share/ca-certificates/bbb.guler.com.crt
update-ca-certificates

Client: Manual